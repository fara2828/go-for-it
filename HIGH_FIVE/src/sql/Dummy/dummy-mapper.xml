<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
	<entry key="loginDummy">
		SELECT 
			USER_NO,
			USER_ID,
			USER_PWD,
			USER_NAME,
			NICKNAME
		FROM 
			MEMBER
		WHERE
			USER_ID = ?
			AND USER_PWD = ?
	</entry>
</properties>
